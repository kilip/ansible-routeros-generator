from __future__ import absolute_import, division, print_function

__metaclass__ = type

{% for resource in resources %}
{% if resource.validated %}
from .{{ resource.import_path }} import {{ resource.className }}Resource
{% endif %}
{% endfor %}


RESOURCE_SUBSETS = dict(
{% for resource in resources %}
    {% if resource.validated %}
    {{ resource.name }}={{ resource.className }}Resource,
    {% endif %}
{% endfor %}
)
