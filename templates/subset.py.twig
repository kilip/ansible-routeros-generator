from __future__ import absolute_import, division, print_function

__metaclass__ = type

{% for resource in resources %}
{% if resource.validated %}
from .{{ resource.package }}.{{ resource.name }} import {{ resource.className }}
{% endif %}
{% endfor %}


RESOURCE_SUBSETS = dict(
{% for resource in resources %}
{% if resource.validated %}
    {{ resource.name }}={{ resource.className }},
{% endif %}
{% endfor %}
)
