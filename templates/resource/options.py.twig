{% for key,option in resource.options  %}
                "{{ key }}": {
{% if option.options is defined %}
                    "options": {{ option.options|json_encode|raw }},
{% endif %}{% if option.type is defined %}
                    "type": "{{ option.type }}",
{% endif %}{% if option.elements is not null %}
                    "elements": "{{ option.elements }}",
{% endif %}{% if option.required is not null %}
                    "required": True,
{% endif %}{% if option.choices is not empty %}
                    "choices": {{ option.choices|json_encode|raw }},
{% endif %}{% if option.default is not null and option.required is null %}
                    "default": {% if option.type == "bool" or option.type == "int" %}{{ option.default }}{% elseif option.default is iterable  %}{{ option.default|json_encode|raw }}{% elseif option.default == 'None' %}None{% else %}"{{ option.default|trim('"') }}"{% endif %},
{% endif %}
                },
{% endfor %}