.. _kilip.routeros.{{ module_name }}_module

********************************
kilip.routeros.{{ module_name }}
********************************

Version Added: **{{ resource.versionAdded }}**

RouterOS Submenu: **{{ resource.command }}**

.. contents::
   :local:
   :depth: 2


{{ "Synopsis"|rst_title }}

{% for description in docs.descriptions %}
-  {{ description }}
{% endfor %}


{{ "Parameters"|rst_title }}

state
  | **choices**: {{ resource.module.states|join(", ") }}
  | **default**: {{ resource.module.default_state }}
  | **required**: False
  | **type**: str
  Available state for this module

config
  | **type**: list
  | **elements**: dict
  A dictionary for `{{ resource.command }}` options described in the table below

.. raw:: html

{% include "docs/parameters.html.twig" %}



{{ "Examples"|rst_title }}

{% for example in resource.examples %}

{{ example.title|rst_subtitle }}

**Before State**
{{ example.pre|console(resource, 2)|example_ssh|raw }}


**Configuration**

{{ example.argument_spec|format_yaml|raw|example_yaml(module_name, example)|raw }}

**Executed Command**

{{ example.commands|example_ssh(2)|raw }}

**After State**

{{ example.post|console(resource, 2)|example_ssh|raw }}

{% endfor %}