.. _kilip.routeros.{{ module_name }}_module

********************************
kilip.routeros.{{ module_name }}
********************************

Version Added: **{{ resource.versionAdded }}**

RouterOS Submenu: **{{ resource.command }}**

.. contents::
   :local:
   :depth: 1


Synopsis
--------
{% for description in docs.descriptions %}
-  {{ description }}
{% endfor %}


Parameters
----------

state
  | **choices**: {{ resource.module.states|join(", ") }}
  | **default**: {{ resource.module.default_state }}
  | **required**: False
  | **type**: str
  Available state for this module

config
  | **type**: list
  | **elements**: dict
  A dictionary for `{{ resource.command }}` options described in the table below

.. raw:: html

{% include "docs/parameters.html.twig" %}



Examples
--------
{% for example in resource.examples %}

{{ example.title }}
  | **Before State**

  ```ssh
{{ example.pre|raw }}
  ```

  | **Configuration**

  ```yaml
- name: {{ example.name }}
  kilip.routeros.{{  module_name }}:
{{ example.argument_spec|format_yaml|raw }}
  ```

  | **Executed Command**
  ```ssh
  {% for command in example.commands %}
{{ command|raw }}
  {% endfor %}

  ```

  | **After State**
  ```ssh
{{ example.post|raw }}
  ```
{% endfor %}