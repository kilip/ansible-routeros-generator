from __future__ import absolute_import, division, print_function

__metaclass__ = type

from ansible_collections.kilip.routeros.plugins.modules import (
    {{resource.module_name}},
)
from .utils import set_module_args
{% if resource.type == "plural" %}
from ansible_collections.kilip.routeros.plugins.module_utils.utils import (
  gen_remove_invalid_resource,
)
from .resource_module import TestResourceModule
{% else %}
from .config_module import TestConfigModule
{% endif %}


{% if resource.type == "plural" %}
class TestROS{{ resource.className }}Module(TestResourceModule):
{% else %}
class TestROS{{ resource.className }}Module(TestConfigModule):
{% endif %}

    module = {{resource.module_name}}
