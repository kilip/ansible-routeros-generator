from __future__ import absolute_import, division, print_function
__metaclass__ = type

"""
Unit Tests For ros_{{ facts.name }} module
"""

from ..facts_base import TestFactsBase


class Test{{ facts.name|classify }}Facts(TestFactsBase):
    def setUp(self):
        self.fixture_file = "{{ facts.package }}.{{ facts.name }}.yaml"
        TestFactsBase.setUp(self)

    def test_ros_facts(self):
        self.do_facts_test()
