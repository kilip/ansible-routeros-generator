dhcp_lease:
  validated: true
  package: ip
  command_root: /ip dhcp-server lease
  generator:
    url: https://wiki.mikrotik.com/wiki/Manual:IP/DHCP_Server
    tableIndex: 4
  options:
    insert_queue_before:
      type: str
      ignore: true
    