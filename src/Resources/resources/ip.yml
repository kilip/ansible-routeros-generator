ip_address:
  url: https://wiki.mikrotik.com/wiki/Manual:IP/Address
  html_id: Properties

ip_dhcp_client:
  url: https://wiki.mikrotik.com/wiki/Manual:IP/DHCP_Client
  html_id: Properties

ip_pool:
  command_root: /ip pool
  options:
    name:
      type: str
    ranges:
      type: str