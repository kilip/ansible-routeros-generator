ip_dhcp_server:
  url: https://wiki.mikrotik.com/wiki/Manual:IP/DHCP_Server
  html_id: General
  options:
    relay:
      type: str
    src_address:
      type: str
    client_mac_limit:
      type: str
    
ip_dhcp_network:
  html_id: Networks
  url: https://wiki.mikrotik.com/wiki/Manual:IP/DHCP_Server
  command_root: /ip dhcp-server network

ip_dhcp_lease:
  url: https://wiki.mikrotik.com/wiki/Manual:IP/DHCP_Server
  html_id: Leases